import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'randomuser.me', 'www.caminodelosjesuitas.com', 'static.tokkobroker.com'],
  },
};

export default nextConfig;
